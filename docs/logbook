# Journal de Bord — Robot Mobile Autonome

**Projet :** Robot Mobile Autonome Pick & Place
**Auteure :** Saida Fatimatou Zahraa Diouf
**Repo :** github.com/fatima02d/mobile-manipulator-robot

---

## 2026-04-27

**Objectif :** Établir la base hardware complète du robot

**Accompli :**
- Châssis Hiwonder assemblé avec moteurs sur M1 et M2
- RPi 4B → Hiwonder : SDA/SCL/5V/GND branchés (I2C)
- HC-SR04 branché avec diviseur de tension 1kΩ/2kΩ sur ECHO
- ESP32 TTGO T-Display V1.1 identifié (TX=GPIO17, RX=GPIO2, SDA=GPIO21, SCL=GPIO22)
- LiPo Zeee 7.4V 2200mAh 50C identifiée comme source alimentation moteurs
- Architecture système complète définie
- Fichiers de documentation créés : README, hardware.md, troubleshooting, IMAGES_GUIDE

**Problèmes :**
- Micro SD standard incompatible avec RPi 4B (nécessite micro SD)
- Connecteur LiPo XT30 incompatible avec port PWR Hiwonder (JST)
- Chargeur LiPo manquant

**Solutions :**
- Chargeur LiPo CHICIRIS TE3AC commandé (inclut adaptateur Deans → JST)
- Liste d'achats finalisée : micro SD 32GB SanDisk

**Prochaine étape :**
- Acheter micro SD 32GB
- Installer Raspberry Pi OS 64-bit
- Tester communication I2C RPi → Hiwonder

---

## 2026-04-28

**Objectif :** Installer OS sur RPi et établir connexion SSH

**Accompli :**
- Micro SD 32GB reçue et flashée avec Raspberry Pi OS 64-bit
- Adaptateur Deans → JST reçu avec le chargeur — LiPo connectée à Hiwonder
- SSH activé lors du flash via Raspberry Pi Imager
- Nom d'hôte : `IndigoWing` / Utilisateur : `indigozafa`
- Connexion SSH établie via données mobiles : `ssh indigozafa@indigowing.local`
- I2C activé via `sudo raspi-config`
- Hiwonder détectée à l'adresse **0x34** via `i2cdetect -y 1`
- Script Python de test moteurs créé et exécuté
- **Robot avance, recule et tourne** — Phase 1 complète ✅

**Problèmes rencontrés :**
- SSH non fonctionnel au premier flash → SSH n'était pas activé correctement → reflasher en vérifiant la case "Accès à distance"
- `hostname indigowing.local` non résolvable → ordi et RPi sur réseaux différents → utiliser données mobiles comme hotspot commun
- Avertissement "Host key has changed" après reflash → `ssh-keygen -R indigowing.local` pour effacer l'ancienne clé
- Court-circuit RPi quand 5V Hiwonder + Powerbank branchés simultanément → règle : une seule source par composant
- Mauvais registres I2C (0x01) → moteurs ne bougent pas → registres corrects trouvés : MOTOR_TYPE=20, POLARITY=21, SPEED=51
- Fils moteurs M1/M2 débranchés → moteurs font du bruit mais ne bougent pas → rebrancher JST fermement

**Solutions :**
- Alimentation RPi : Powerbank Podoru via USB-C uniquement
- Alimentation moteurs : LiPo 7.4V → Hiwonder PWR directement
- Registres Hiwonder confirmés :
  - `I2C_ADDR = 0x34`
  - `MOTOR_TYPE_ADDR = 20`
  - `MOTOR_ENCODER_POLARITY_ADDR = 21`
  - `MOTOR_FIXED_SPEED_ADDR = 51`

**Code moteurs fonctionnel :**
```python
import smbus2, time
I2C_ADDR = 0x34
MOTOR_TYPE_ADDR = 20
MOTOR_ENCODER_POLARITY_ADDR = 21
MOTOR_FIXED_SPEED_ADDR = 51
bus = smbus2.SMBus(1)
bus.write_byte_data(I2C_ADDR, MOTOR_TYPE_ADDR, 3)
bus.write_byte_data(I2C_ADDR, MOTOR_ENCODER_POLARITY_ADDR, 0)
# Avancer : [100, 100, 0, 0]
# Reculer : [-100, -100, 0, 0]
# Gauche  : [-100, 100, 0, 0]
# Droite  : [100, -100, 0, 0]
# Stop    : [0, 0, 0, 0]
bus.write_i2c_block_data(I2C_ADDR, MOTOR_FIXED_SPEED_ADDR, [100, 100, 0, 0])
```

**Prochaine étape :**
- Phase 2 — Téléopération Xbox via ESP32 Bluetooth
- Écrire code ESP32 pour recevoir commandes manette
- Établir communication UART ESP32 → RPi
- LM2596 buck converter commandé pour alimentation mobile RPi

---

## Template sessions futures

```
## YYYY-MM-DD

**Objectif :**

**Accompli :**
-

**Problèmes :**
-

**Solutions :**
-

**Prochaine étape :**
-
```

---

## 2026-06-15

**Objectif :** Programmer l'ESP32 pour recevoir les commandes Xbox via Bluetooth et les transmettre au RPi via UART

**Accompli :**
- Projet PlatformIO créé dans VS Code (board : lilygo-t-display-s3)
- Versions de librairies correctes identifiées via ancien projet GEI-1049 :
  - XboxSeriesXControllerESP32_asukiaaa@^1.1.1
  - NimBLE-Arduino@^2.3.6
- Code ESP32 écrit avec FreeRTOS (XboxTask a 50Hz)
- Manette Xbox connectée via Bluetooth a l'ESP32
- Valeurs joysticks varient correctement dans le Serial Monitor quand on bouge les joysticks
- Branchements UART confirmes : ESP32 GPIO17(TX) -> RPi Pin10(RX), GPIO2(RX) -> RPi Pin8(TX), GND -> RPi Pin14

**Problemes rencontres :**
- ESP32-BLE-Gamepad incompatible avec NimBLE -> supprimee
- Bluepad32 non disponible via lib_deps PlatformIO -> abandonnee
- XboxSeriesXControllerESP32 incompatible avec NimBLE 1.4.x et 2.5.x -> solution : NimBLE 2.3.6 (version du projet GEI-1049)
- Board incorrecte (lilygo-t-display vs lilygo-t-display-s3) -> corrigee dans platformio.ini
- Warning "REMOTE HOST IDENTIFICATION HAS CHANGED" au reconnexion SSH -> solution : ssh-keygen -R indigowing.local

**Solutions :**
- platformio.ini final :
  board = lilygo-t-display-s3
  lib_deps = h2zero/NimBLE-Arduino@^2.3.6 + asukiaaa/XboxSeriesXControllerESP32_asukiaaa@^1.1.1
- Code ESP32 base sur architecture FreeRTOS de l'ancien projet GEI-1049, adapte pour UART au lieu de I2C vers STM32

**Prochaine etape :**
- Installer pyserial sur RPi
- Ecrire script Python RPi pour recevoir UART et envoyer I2C a Hiwonder
- Tester controle complet Xbox -> ESP32 -> RPi -> Hiwonder -> moteurs
